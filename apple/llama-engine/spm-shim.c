/*
 * Empty translation unit kept solely so SwiftPM accepts this directory as
 * a regular `.target`. The actual symbols come from the static archives
 * built by `apple/scripts/build-static-libs.sh` and linked through
 * `linkerSettings(.unsafeFlags(...))` in the root Package.swift when
 * `LLAMA_ENGINE_STATIC_LIBS_DIR` is set.
 */
